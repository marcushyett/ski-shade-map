# Claude Code Instructions

## Code Quality Requirements

### TypeScript
- Always run `npx tsc --noEmit` after making changes to verify there are no TypeScript errors
- Fix all TypeScript errors before committing code
- Do not ignore or suppress TypeScript errors without explicit user approval
